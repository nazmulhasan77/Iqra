For a fully offline first launch, place the multilingual Whisper Tiny model here in the project as:

lib/assets/models/ggml-tiny.bin

Then declare it in pubspec.yaml under flutter/assets.

This ZIP contains only the requested lib folder, so the 77.7 MB model binary is not embedded here.
